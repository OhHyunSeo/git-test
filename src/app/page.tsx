import LoginContainer from "@/components/LoginContainer";

export default function Home() {
  return (
    <main className="w-full h-full bg-login-background bg-cover bg-center">
      <LoginContainer />
    </main>
  );
}
